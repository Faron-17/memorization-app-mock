import React from 'react'

const page = () => {
  return (
    <main className="px-24 mt-24">
      <p>Create Category</p>
      <p>Form</p>
      <p>: Category Name(input)</p>
      <p>: Pin(check box, max total pinned category is 5 otherwise hidden)</p>
      <p>Submit button(direct to /[id])</p>
    </main>
  )
}

export default page